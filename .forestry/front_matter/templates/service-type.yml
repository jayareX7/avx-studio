---
label: Service Type (page)
hide_body: false
display_field: title
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: thumbnail
  type: file
  config:
    maxSize: 64
  label: Thumbnail
- name: service_description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Service Description
- name: type_of_service_tags_
  type: include
  config: {}
  template: tags
  label: Type of Service(tags)
